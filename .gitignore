# deps
node_modules/

# build output
dist-electron/
release/
release-final/
release-next/

# 词库快照（24MB，构建产物，由 npm run refresh:library 重新生成）
build/library/

# 抓取临时目录（脚本自动写入，不入库）
.scrape/

# 六个工具包本体（377MB，不入库；构建时由 electron-builder 打入 resources/packs）
packed-packs/

# logs & temp
*.log
npm-debug.log*
vite.config.mjs.timestamp-*.mjs

# os
.DS_Store
Thumbs.db
desktop.ini
